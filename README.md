# SmartCameraParkingSystem

## Firebase Setup

In main directory, install dependencies:

``` 
pip install firebase-admin 
```

In the relevant files, to use the firebase API, add the following:

``` 
from firebaseApi import *
```


